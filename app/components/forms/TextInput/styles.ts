import { StyleSheet } from 'react-native';
import { width } from '../../../utils';

export const styles = StyleSheet.create({
  input: {
    height: 50,
    width: width,
    marginLeft: -10,
    paddingLeft: 10,

    backgroundColor: '#fff',
    fontSize: 24,
  },
});
